// distruct the user data
// const {currentUser} = useSelector(state=>state.user.currentUser)
// use the useEffect hook with he useHistory hook
// like
// const history = useHistory()
// useEffect(()=>{ if(currentUser){ history.push("/result"=> the path where you want the user to go if he exists) } },[])=>app.js
// or just add a condition
//  { currentUser ? <Redirect to="/result" /> : <Home/>} =>app.js
